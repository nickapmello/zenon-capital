# Project Rules - Zenon Capital

- **Automatic Git Push to Vercel**: Whenever code files in this repository are created, modified, or updated, automatically stage (`git add .`), commit with a descriptive message (`git commit -m "..."`), and push (`git push`) to `main` so that Vercel deploys the changes immediately.
